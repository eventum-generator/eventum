---
description: Commit changes matching a scope description with a conventional message.
argument-hint: "<scope, e.g. 'all', 'specific namespace files', 'some docs page'>"
---

Three sibling repos share this workspace: `.` (eventum), `../docs/`, `../content-packs/`. Any of them can have pending changes - check all three and commit in each one separately.

Scope: **$ARGUMENTS**.
