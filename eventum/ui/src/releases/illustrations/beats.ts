/**
 * The beats of a scene, in per cent of its loop.
 *
 * One loop lasts the `--ev-cycle` of `scene.css`. The cursor lands on
 * the first beat and, when a scene needs a second move, on the second;
 * everything the interface answers with is placed on one of them, which
 * is what keeps a scene reading as a single take.
 */

export const CLICK = 24;
export const SECOND_CLICK = 58;
