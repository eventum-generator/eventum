input:
  - static:
      count: ${params.event_count}

event:
  script:
    path: ../base/produce_events.py

output:
  - opensearch:
      hosts:
        - ${params.opensearch_url}
      username: admin
      password: admin
      index: ${params.index}
      verify: false
